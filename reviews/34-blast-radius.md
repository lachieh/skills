# Review 34: Blast Radius

Status: approved and implemented

Implementation: `.apm/skills/blast-radius/SKILL.md`

## Verdict

Keep the source workflow for finding indirect breakage and proving the central
safety fact with executable evidence.

The user elected to keep the source workflow with host/model adaptation.

## Approved Changes

- Keep the confidence ladder, indirect-contract search, risk assessment, and
  proof-oriented return format.
- Resolve source readers through `configure-models` with the `exploration` role
  at `S` size.
- Resolve proof implementation through `configure-models` with the
  `implementation` role at `M` size.
- Use `arena` for broad or consequential investigations.
- Make the boss own scope, synthesis, proof inspection, and final claims.
